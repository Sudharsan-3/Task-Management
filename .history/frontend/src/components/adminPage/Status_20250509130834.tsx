import React from 'react'

const Status = () => {
  return (
    <div>
        <button onClick={clickHande} value="Draft">Draft</button>
        <button onClick={clickHande} value="Completeded">Completeded</button>
      
    </div>
  )
}

export default Status
