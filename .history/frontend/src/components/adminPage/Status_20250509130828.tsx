import React from 'react'

const Status = () => {
  return (
    <div>
        <button onClick={} value="Draft">Draft</button>
        <button onClick={} value="Completeded">Completeded</button>
      
    </div>
  )
}

export default Status
