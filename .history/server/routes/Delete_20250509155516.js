import { PrismaClient } from "@prisma/client";

const prisma = PrismaClient();

export const DeleteTasks = ((req,res)=>{
const {id} = req.boddy

    try {
        
        
    } catch (error) {
        
    }
})
    

