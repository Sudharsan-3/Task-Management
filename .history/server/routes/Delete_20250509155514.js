import { PrismaClient } from "@prisma/client";

const prisma = PrismaClient();

export const DeleteTasks = ((req)=>{
const {id} = req.boddy

    try {
        
        
    } catch (error) {
        
    }
})
    

