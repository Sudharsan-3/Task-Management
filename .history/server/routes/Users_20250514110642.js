import { Prisma } from "../generated/prisma";

const prisma = Prisma()

export const User = as(req,res)=>{

    try {
        const response = 
        
    } catch (error) {
        
    }

}
